import React from 'react';
import CartDisplay from '../../organisms/CartDisplay/CartDisplay';

const Cart = () => {
  return (
    <div>
      <CartDisplay />
    </div>
  );
};

export default Cart;
